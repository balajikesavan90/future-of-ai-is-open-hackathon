# Sample Bundle Figure Gap

The checked-in sample research bundle currently demonstrates generated code, tool outputs, trace export, context export, methods text, limitations, and citation metadata.

It does not currently include an exported figure artifact because the checked-in sample trace output is tabular JSON, not an image payload. A future sample trace should use a plotting prompt so `figures/` contains a real PNG without adding fragile manual image-generation steps.
