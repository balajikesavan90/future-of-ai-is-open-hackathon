# Documentation Assets

Preferred local-flow image:

```text
docs/assets/local_flow.png
```

Capture the flow:

```text
Analyze Data -> Tips Dataset -> Save Data Dictionary -> prompt -> generated code -> Export Research Bundle
```

Keep the image small enough for repository review. A short GIF is acceptable if it does not materially bloat the repo.
